# JMW Gear Spec Navigator (V2.0)

JMW 전 제품군(이미용가전 / 생활가전 / 주방가전) 스펙 비교·검색 사내 도구입니다.
직원들이 매장/사무실에서 브라우저로 접속해 모델을 검색하고, 스펙을 나란히 비교할 수 있습니다.

## 구성

```
index.html      메인 페이지
style.css       디자인/레이아웃
app.js          검색·필터·비교 로직
products.json   제품 데이터 (엑셀에서 자동 변환됨, 221개 모델)
images/         제품 실사진 (엑셀에서 자동 추출, webp 변환)
```

## GitHub Pages에 올리는 방법 (최초 1회)

1. github.com 에서 새 저장소(Repository) 생성 — 예: `jmw-gear-spec-navigator`
   - Public 또는 Private 모두 가능하나, Pages 무료 기능은 Public 저장소에서 가장 간단합니다.
   - 사내용으로 비공개가 필요하면 Private 저장소 + GitHub 유료 플랜(Pages 지원) 또는 Organization 사용을 검토하세요.
2. 저장소 페이지의 **Add file → Upload files** 클릭
3. 이 폴더 안의 파일/폴더(`index.html`, `style.css`, `app.js`, `products.json`, `images/`)를 통째로 드래그 앤 드롭 업로드 후 **Commit changes**
4. 저장소 **Settings → Pages** 이동
5. **Source**를 `Deploy from a branch`로, **Branch**를 `main` / `(root)`로 설정 후 저장
6. 1~2분 후 `https://[깃헙아이디].github.io/jmw-gear-spec-navigator/` 형태의 주소로 접속 가능

## 데이터 업데이트 방법 (신모델 추가 등)

이 도구는 엑셀 파일(`JMW_모델_라인업.xlsx`)에서 자동 변환한 정적 데이터를 사용합니다.
엑셀이 업데이트되면 Claude에게 "엑셀 최신본으로 데이터 갱신해줘"라고 요청하시면
`products.json`과 `images/`를 재생성해 드립니다. (버전은 V1.0 → V1.1 등으로 관리)

## 버전 기록

- **V2.13** (2026.08) — 유통 값 완전 정규화 + 비교창 마무리 개선.
  - 유통 값을 "A+B+C" 형태로 완전히 정규화 — 엑셀 셀 안에 줄바꿈·슬래시·쉼표가 뒤섞여 있어도(예: "MASS\n온라인직영, 특판, 홈쇼핑") 모두 인식해서 "MASS+온라인직영+특판+홈쇼핑"처럼 하나의 값으로 통일
  - 빠른 선택 칩에 줄바꿈 방지 적용 (긴 유통 값이 두 줄로 쪼개져 보이던 문제 해결)
  - 비교표 "더 좋은 값" 하이라이트를 진한 초록 배경 + 굵은 글씨 + 좌측 강조선으로 훨씬 눈에 띄게 개선
  - 비교창 헤더를 세로 정렬 구조로 변경해 모델명 줄수가 달라도 "비교에서 제외" 버튼이 항상 같은 위치에 정렬
  - 비교창 전체 폭 최대 1720px로 재확장
- **V2.12** (2026.08) — 유통 필터 원본 값 방식 전환 + 비교창 대개편.
  - 유통 필터를 엑셀 "유통" 컬럼 원본 문구 그대로 선택지로 표시 (예: "MASS / 온라인직영"을 하나의 값으로) — 복수 선택 시 OR
  - 비교창 전체 폭 확장(최대 1560px) — 6개 비교 시 잘리던 문제 해결
  - 비교창 크기는 선택 개수(2~6개)와 무관하게 고정, 모델별 칸 너비만 균등 자동 배분
  - 비교표에 "더 좋은 값" 자동 하이라이트(▲) 추가 — 무게는 낮을수록, 코드길이·풍온·풍속·와트·온도범위·스위치 단계는 높을수록 좋은 것으로 자동 판단
- **V2.11** (2026.08) — 유통 필터 + 비교창 개선.
  - "유통" 빠른 선택 섹션 추가 (드라이기 8종/아이론 4종/컬링아이언 2종 채널)
  - 유통 필터는 전용 `distChannels` 필드로 정확히 매칭 — 검증 과정에서 제품명에 "PRO"가 포함된 5개 제품이 실제 유통과 무관하게 잘못 걸릴 뻔한 것을 발견해 수정
  - 비교창의 모델별 삭제 버튼("✕ 비교에서 제외")을 더 눈에 띄게 개선 (기능 자체는 이전부터 있었음)
- **V2.10** (2026.08) — 드라이기 프리볼트 키워드에 FREEGO(프리고, MVF6A01A) 추가. 국내 드라이기는 엑셀에 전압 정보 컬럼 자체가 없어 "100-240V" 문구로 자동 판별이 불가능한 상태 — 앞으로 확인되는 프리볼트 모델은 수동으로 등록하는 방식입니다. 정확도를 위해서는 엑셀에 국내 드라이기용 전압/프리볼트 여부 컬럼을 추가하시는 걸 권장드립니다.
- **V2.9** (2026.08) — 필터 UX 정리 + 단계 필터 추가.
  - "정렬 기준"/"빠른 선택"/"시리즈"를 각각 라벨과 구분선으로 명확히 분리
  - 드라이기 스위치 단수(2~6단), 아이론 온도 단계(1~16단) 빠른 선택 칩 추가 — 텍스트 substring이 아닌 정확한 숫자 추출이라 "11단"이 "1단" 조건에 잘못 걸리는 일 없음
  - 단계 칩끼리는 OR, 다른 조건과는 AND로 결합
  - 블로우셋/블로우셋 프로 → "블로우셋", 케어부스터/케어부스터FV → "케어부스터"로 시리즈 통합
- **V2.8** (2026.08) — 상단바에 쇼핑몰 바로가기 메뉴 5개 추가 (JMW 공식몰, JMW 네이버, 엘리첸 네이버, 바이툴즈 공식몰, 바이툴즈 네이버). 클릭 시 새 탭으로 바로 이동합니다. 모델별 개별 상품 링크 자동 매칭은 정확도 문제로 보류했으며, 상품코드+URL 매핑 엑셀을 받으면 정확히 연결할 수 있습니다.
- **V2.7** (2026.08) — 5가지 개선사항 반영.
  - 드라이기 "프리볼트" 키워드 추가 (100-240V 겸용 SDF4B1A, SDMF4A1A)
  - 빠른 키워드 다중 선택 지원 (AND 조합)
  - 카드 표시 사양 확장: 드라이기(풍온) / 아이론·바디드라이어(온도범위) / 서큘레이터(배터리용량·풍속, 기능 텍스트에서 자동 추출) / 음식물처리기(제품무게·온도범위)
  - 비교함에 "인사이트 & 추천" 섹션 추가: 각 모델 실제 사양·태그를 근거로 장점/추천 상황 자동 요약 + 선택 모델 간 상대 비교(가장 가벼움/출력 최고/코드 최장)
  - 계열/라인별 그룹 필터 행 추가 (국내 제품은 계열, 소싱 제품은 품명, 생활가전은 품번 접두어 기준 라인 — 바디드라이어/에어뮤즈/미니 선풍기)
- **V2.6** (2026.08) — 우클릭 방지 + F12/Ctrl+Shift+I 등 개발자도구 단축키 차단 (완전한 보안 차단은 아니며, 일반적인 캐주얼한 복사/열람 시도를 막는 수준입니다).
- **V2.5** (2026.08) — 버전 정보 패널 개선: 최신 2개 버전은 자동으로 펼쳐서 바로 보이고, 그 이전 버전은 "이전 버전 더보기"로 접어서 표시.
- **V2.4** (2026.08) — 좌측 사이드바에 "버전 정보" 패널 추가 (현재 버전 + 버전별 업데이트 내역을 접고 펼쳐서 확인 가능). 이후 이 패널이 가장 정확한 최신 이력입니다.
- **V2.3** (2026.08) — "단종 모델 포함" 기본값을 ON으로 변경 (처음 접속 시 판매중+단종 모델 전체가 보이며, 필요 시 토글로 판매중만 필터링). 이후 버전부터는 `index.html` 상단 HTML 주석에도 버전이 표시됩니다.
- **V2.2** (2026.08) — 데이터 정확도 재검증 + UX 정리.
  - **[데이터] 전 항목 재검증**: 엑셀 7개 시트의 모델 수(221개)·스펙 컬럼(전부 포함, 누락 0)·제품 이미지(221개 전부 매칭) 재확인 완료
  - **[확인] 서큘레이터 4종 정상**: "에어뮤즈 소형/벨로"는 엑셀상 단종 표시라 기본 화면에 숨겨져 있었을 뿐, 데이터 누락 아님 (단종 포함 토글로 확인 가능)
  - **[버그] "신제품" 키워드 삭제**: 클릭해도 결과 없던 죽은 키워드 제거
  - **[UX] 레이아웃 재배치**: 좌측 사이드바의 "스펙 정밀 정렬"·"빠른 키워드 추천"을 상단(그리드 위)으로 이동, 한 영역에서 같이 조작 가능
  - **[UX] 키워드 전면 재설계**: 실제 데이터 빈도 분석 기반으로 전 중분류(Dryer/Iron/Culing Iron/Circulator 등)에 스펙 정밀 정렬 + 키워드를 확충하여 상단 한 줄이 꽉 차도록 구성
- **V2.1** (2026.08) — Iron/Culing Iron/생활가전 "제어방식·안전장치" 등 사양 누락분 반영, 키워드-검색 충돌 버그 수정, 프리볼트 키워드 추가, 화면 폭 확장.
- **V2.0** (2026.08) — 대규모 개편. 전 카테고리 통합 검색, 대분류+중분류 사이드바 트리, 최신순 기본정렬,
  드라이기 스펙 정밀 정렬, 화이트/반투명 글래스모피즘 리뉴얼.
- **V1.0** (2026.08) — 최초 배포. 이미용가전(Dryer/Iron/Culing Iron), 생활가전(Body Dryer/Circulator),
  주방가전(Bytulz) 전 라인업 221개 모델 · 실사진 · 키워드 검색·추천 · 최대 6개 비교 · 단종/타입 필터 반영.
